#include "toml.h"
#include <stdio.h>

int main(int argc, char **argv) {
        if (argc == 1) {
                file = fopen(argv[0], "r");
                toml_table_t
        } else {
                fprintf(stderr, "Provide a toml file");
                return EXIT_FAILURE;
        }
}
